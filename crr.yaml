services:
  php:
    build:
      context: ./
      dockerfile: ./docker/php/Dockerfile.rr
    working_dir: /app
    environment:
      APP_TITLE: ${UNITMAN_APP_TITLE:-defaultTitle}
      RR_NUM_WORKERS: ${UNITMAN_INTEGER_FIELD:-3}
    ports:
      - "8081:8080"

